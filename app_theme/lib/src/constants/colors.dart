import 'package:flutter/widgets.dart';

/// A class that contains constants for commonly used colors in the app.
abstract final class AppColors {
  /// Solid Black
  static const black = Color.fromARGB(255, 0, 0, 0);

  /// Solid White
  static const white = Color.fromARGB(255, 255, 255, 255);

  /// App's Primary Dark Red Color
  static const darkRed = Color.fromARGB(255, 176, 8, 8);

  /// App's Primary Dark Red Color
  static const lightRed = Color.fromARGB(255, 244, 67, 54);

  /// Light Grey
  static const lightGrey = Color.fromARGB(255, 144, 144, 144);

  /// Off White
  static const offWhite = Color.fromARGB(255, 239, 239, 239);

  /// Background dark grey, used for scaffolds background.
  static const darkGrey = Color.fromARGB(255, 18, 18, 18);

  /// Card dark drey used of cards and list tiles
  static const cardDarkGrey = Color.fromARGB(255, 30, 30, 30);

  /// Transparent
  static const transparent = Color.fromARGB(0, 0, 0, 0);
}
