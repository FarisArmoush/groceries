import 'package:flutter/material.dart';

class SendVerificationEmailBottomSheet extends StatelessWidget {
  const SendVerificationEmailBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
