import 'package:flutter/material.dart';

class AppTransparentButton extends StatelessWidget {
  const AppTransparentButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
