import 'package:flutter/material.dart';

class RecipeCardImage extends StatelessWidget {
  const RecipeCardImage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
